<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musilicious">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayingActivity" android:label="@string/playing"/>
        <activity android:name=".AboutActivity" android:theme="@style/AppTheme.ActionBar" android:label="@string/about"/>
        <activity android:name=".PaymentActivity" android:theme="@style/AppTheme.ActionBar" android:label="@string/buy"/>
    </application>

</manifest>